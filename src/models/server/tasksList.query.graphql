query TasksList {
  tasksList {
    items {
      id
      sequence
      name
      type
      priority
      serviceLine {
        id
        industryId
        name
        description
      }
      description
      subjects {
        ... on Site {
          id
          name
          address {
            street
            locality
            region
            postalCode
            country
          }
          position {
            latitude
            longitude
            altitude
          }
        }
      }
      workflow {
        id
        stepId
      }
    }
    nextToken
    scannedCount
  }
}
